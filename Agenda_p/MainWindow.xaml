<Window x:Class="Agenda_p.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Agenda_p"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="#C26B6B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Menu latéral -->
        <StackPanel Background="#EAB8B8" VerticalAlignment="Stretch" Grid.Column="0">
            <Ellipse Width="60" Height="60" Fill="#C26B6B" HorizontalAlignment="Center" Margin="10"/>

            <Button Margin="10" Width="180" Height="30" Style="{StaticResource RoundedButtonStyle}" Name="BTN_Accueil" Click="BTN_Accueil_Click">
                <StackPanel Orientation="Horizontal">
                    <Image Height="20" Width="20" Source="/Resources/home.png" />
                    <Label Content="Accueil" FontFamily="Dubai" FontWeight="Bold" />
                </StackPanel>
            </Button>

            <Button Margin="10" Width="180" Height="30" Style="{StaticResource RoundedButtonStyle}" Name="BTN_Evenements" Click="BTN_Evenements_Click">
                <StackPanel Orientation="Horizontal">
                    <Image Height="20" Width="20" Source="/Resources/if_piste.png" />
                    <Label Content="Evènements" FontFamily="Dubai" FontWeight="Bold" />
                </StackPanel>
            </Button>

            <Button Margin="10" Width="180" Height="30" Style="{StaticResource RoundedButtonStyle}" Name="BTN_Contacts" Click="BTN_Contacts_Click">
                <StackPanel Orientation="Horizontal">
                    <Image Height="20" Width="20" Source="/Resources/if_user.png" />
                    <Label Content="Contacts" FontFamily="Dubai" FontWeight="Bold" />
                </StackPanel>
            </Button>

            <Button Margin="10" Width="180" Height="30" Style="{StaticResource RoundedButtonStyle}" Name="BTN_ToDoList" Click="BTN_ToDoList_Click">
                <StackPanel Orientation="Horizontal">
                    <Image Height="20" Width="20" Source="/Resources/todolist.png" />
                    <Label Content="A faire" FontFamily="Dubai" FontWeight="Bold" />
                </StackPanel>
            </Button>

            <Button Margin="10" Width="180" Height="30" Style="{StaticResource RoundedButtonStyle}" Name="BTN_Messages" Click="BTN_Messages_Click">
                <StackPanel Orientation="Horizontal">
                    <Image Height="20" Width="20" Source="/Resources/messages.png" />
                    <Label Content="Messages" FontFamily="Dubai" FontWeight="Bold" />
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- Contenu principal avec Frame pour navigation -->
        <Grid Grid.Column="1" Background="SeaShell" x:Name="Window_content">
            <StackPanel Margin="20">
                <!-- Barre de recherche -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <Grid>
                        <TextBox Width="300" Height="30" Margin="0,0,10,0"/>
                        <TextBlock Text="Recherche..." 
                                   Foreground="Gray" 
                                   VerticalAlignment="Center" 
                                   Margin="5,0,0,0" 
                                   IsHitTestVisible="False" />
                    </Grid>
                    <Button Content="🔍" Width="30" Height="30"/>
                </StackPanel>

                <!-- Titre -->
                <TextBlock Text="Tous" FontSize="24" FontWeight="Bold" VerticalAlignment="Top" Margin="0,0,0,20"/>

                <!-- Frame pour afficher les pages -->
                <Frame x:Name="MainFrame" Grid.Column="1" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>